# No role assignments needed — the API authenticates to Blob Storage using
# the storage account key (STORAGE_ACCOUNT_KEY app setting) instead of
# a managed identity. This is compatible with the Free tier of Static Web Apps.
